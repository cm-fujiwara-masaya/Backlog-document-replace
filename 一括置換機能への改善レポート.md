# Backlogドキュメント検索・置換の拡張機能を「一括置換」対応に改善してみた

## はじめに

以前、下記の記事を参考に、Backlogドキュメント上でテキストの検索・置換ができるChrome拡張機能を作成しました。

[Backlogドキュメントで検索・置換ができるChrome拡張機能を作ってみた](https://dev.classmethod.jp/articles/backlog-document-find-replace/)

元記事の拡張機能は「開いている1ページの中で検索・置換する」という単一ページ向けの機能でした。今回は、これをベースに実際の運用で必要になった「プロジェクト内の複数ドキュメントをまとめて置換したい」というニーズに応える形で改善したので、その内容をまとめます。

## きっかけ

元記事の拡張機能は個人で使う分には十分でしたが、実際に運用保守のドキュメント群（数百件規模）で命名規則の変更や用語統一を行う場面では、1ページずつ開いて検索・置換を繰り返すのは現実的ではありませんでした。

- プロジェクト配下の全ドキュメント、あるいは特定フォルダ配下のドキュメントをまとめて対象にしたい
- 置換前にどのドキュメントに何件ヒットするか確認してから実行したい（ドライラン）
- 実行中の進捗が見えて、途中でキャンセルできるようにしたい

この3点を満たすため、単一ページ版の拡張機能に「一括置換モード」を追加することにしました。

## やったこと

### 1. 一括置換モードの追加

ポップアップに「このページ」「一括置換」の2タブを設け、一括置換タブでは以下の操作フローを実装しました。

1. スペースURL・APIキー・プロジェクトキーを入力し、Backlog APIでドキュメントツリーを取得
2. 対象フォルダ（またはプロジェクト全体）を選択
3. 検索文字列・置換文字列を入力し、「ドライラン」で各ドキュメントの一致件数を事前確認
4. 「一括置換を実行」で、一致したドキュメントを順番に処理

実装のポイントは、Backlogにはドキュメントを直接更新するAPIが提供されていないことです。そこで、拡張機能が裏側で以下を自動化しています。

- `background.js`（新規追加）: Backlog APIでツリー取得・ドライランを行い、対象タブに対して「サイドバーのドキュメントリンクをクリック→編集モードに入る→全置換→保存」という一連の操作の実行を指示し、進捗を`chrome.storage.local`に保存してポップアップに反映
- `content.js`: サイドバーの該当ドキュメントリンクを探して自動クリックし、SPA遷移の完了を待ってから編集ボタン・保存ボタンを自動操作する処理（`navigateAndAutoReplace` / `autoReplace` / `findSidebarLink` など）を追加

これに伴い、`manifest.json`には`background`（サービスワーカー）と、API呼び出し・タブ操作に必要な`storage` / `scripting` / `tabs`権限、Backlogドメインへの`host_permissions`を追加しました。元記事版は`activeTab`権限のみで完結する作りでしたが、複数ドキュメントを横断する都合上、権限範囲を広げる形になっています。

### 2. ノードをまたいだ検索・置換への対応

元記事版の検索・置換は、1つのテキストノード内でしか一致を検出できませんでした。そのため、リンクや太字などでテキストが複数のDOMノードに分割されている箇所（例:「SNOWFLAKE」がハイライト用の`<span>`で囲まれているセルなど）を跨いだ検索ができないケースがありました。

これを解消するため、`findTextAcrossNodes` / `selectAndReplaceRange`という関数を追加し、段落・見出し・テーブルセルなどのブロック単位でテキストノードを連結してから検索する方式に変更しました。これにより、装飾で分断された文字列も1つの文字列として検索・置換できるようになっています。

## 発生した不具合と対処

### 表（テーブル）のレイアウトが崩れる不具合

一括置換・全置換を実行すると、Backlogドキュメント内の表の列構成が崩れる不具合が発生しました。調査の結果、原因は次の通りでした。

Backlogのリッチテキストエディタは`ProseMirror`というライブラリで実装されており、DOMの変更を`MutationObserver`（非同期）で検知して内部データモデルと同期する仕組みになっています。ところが、全置換処理（`replaceAllText`）は、事前に集めた全ての一致箇所に対して`execCommand("insertText")`を待機なしで連続実行していました。

```js
// 修正前: 待機なしで全マッチを連続実行
const reversedMatches = [...matches].reverse();
for (const match of reversedMatches) {
  selectAndReplaceRange(match, replaceText); // execCommandを即座に連続実行
}
```

この待機なしの連続実行により、ProseMirrorのMutationObserverが1件ずつ変更を処理する前に複数のDOM変更が積み重なってしまい、テーブルのようにセル構造が厳密なノードで変更の対応関係を誤って解釈し、表のレイアウトが崩れることが分かりました。なお、この問題は今回の改造で新たに生まれたものではなく、**参考記事のコードに元々存在していた設計上の問題**でした（単一ページのみを対象にしていた元記事版では表面化しにくかったものの、同じ処理パターンを一括置換に流用したことで顕在化した形です）。

対処として、`replaceAllText`を非同期化し、以下のように変更しました。

```js
// 修正後: 1件処理するたびにDOM同期を待ってから次を検索
for (let i = 0; i < initialCount; i++) {
  const matches = findTextAcrossNodes(searchText, caseSensitive); // 都度ライブDOMから再検索
  const match = matches[matches.length - 1];
  selectAndReplaceRange(match, replaceText);
  await waitForDomSettle(); // ProseMirrorの再描画完了を待つ
}
```

1件置換するたびにProseMirrorの再描画（`requestAnimationFrame`を2回＋短い待機）を待ってから、次のマッチをライブDOMから再検索する方式にしたことで、表の崩れを解消しました。あわせて、置換後文字列に検索文字列が含まれる場合（例: 「a」→「aa」）に無限ループしないよう、事前に数えた件数分だけ処理する形にしています。

### 開発中に踏んだその他の不具合

一括置換の自動操作を作り込む過程では、次のような不具合にも当たりました。

- ドキュメントURLのパターン誤り（`/projects/{key}/document/{id}` と誤認していたが、実際は`/document/{projectKey}/e/{documentId}`）
- content scriptがページにまだ注入されていないタイミングでメッセージを送ってしまう問題 → `waitForContentScript`でpingをリトライし、応答がなければ`chrome.scripting.executeScript`で直接注入するフォールバックを追加
- `sleep()`関数を`background.js`側にしか定義しておらず、`content.js`側のスコープで参照できずエラーになっていた（`content.js`にも同名の関数を追加して解消）

## Before / After 比較

| 項目 | 元記事版 | 今回の改善版 |
| --- | --- | --- |
| 対象範囲 | 開いている1ページのみ | 1ページ、またはプロジェクト内の複数ドキュメントを一括 |
| 検索対象 | 単一テキストノード内のみ | ブロック単位でノードをまたいだ文字列も検索可能 |
| 事前確認 | なし | ドライランで一致件数を事前確認可能 |
| 進捗表示 | なし | 進捗バー・結果一覧・キャンセルに対応 |
| 権限 | `activeTab`のみ | `activeTab` / `tabs` / `storage` / `scripting` / Backlogへの`host_permissions` |
| 全置換の安定性 | 待機なしの連続置換（テーブルが崩れる場合あり） | 1件ごとにDOM同期を待って処理（表崩れを解消） |

## まとめ

元記事の拡張機能をベースに、実運用で必要になった「複数ドキュメントへの一括置換」「ドライラン」「進捗管理」を追加し、あわせて全置換時にテーブルが崩れる不具合を修正しました。単一ページの検索・置換という土台があったからこそ、一括置換への拡張やDOM操作まわりの不具合調査もスムーズに進められたと感じています。

引き続き使いながら、気になる部分があれば改善していく予定です。
